# StreamWeaver Dashboard

The React monitoring dashboard is implemented in the managed `client/` workspace so it can use the project’s authenticated API and dashboard components. This directory is retained as a stable architectural boundary for a future standalone dashboard build.
